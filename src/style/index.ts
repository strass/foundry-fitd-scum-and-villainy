import { css } from "emotion";

export const unstyleList = css({
  listStyle: "none",
  margin: 0,
  padding: 0,
});
export const horizontalList = css({
  display: "flex",
});
export const smallCaps = css({ fontVariant: "all-small-caps" });
export const bold = css({ fontWeight: 700 });
export const flexRow = css({ display: "flex", flexDirection: "row" });
